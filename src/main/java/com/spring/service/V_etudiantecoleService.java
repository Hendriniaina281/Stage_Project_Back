 package com.spring.service;

 import org.springframework.beans.factory.annotation.Autowired;
 import org.springframework.stereotype.Service;
 import com.spring.model.V_etudiantecole;
 import com.spring.repository.V_etudiantecoleRepository;
 import java.util.List;
 import java.util.Optional;

 /*
    File generated By
    Christooj_Generation_Code
 */
 @Service
 public class V_etudiantecoleService {
    private final V_etudiantecoleRepository repo;

    public List<V_etudiantecole> findEnfant(int idetudiant) {
      return repo.findByIdetudiant(idetudiant);
   }

    public List<V_etudiantecole> findEtudiantecole(int idecoleclasse,int idanneescolaire) {
      return repo.findEtudiantEcole(idecoleclasse,idanneescolaire);
   }

    @Autowired
    public V_etudiantecoleService(V_etudiantecoleRepository rep) {
        this.repo = rep;
    }

    public V_etudiantecole create(V_etudiantecole object) {
       return repo.save(object);
   }


    public List<V_etudiantecole> read() {
       return repo.findAll();
    }

    public Optional<V_etudiantecole> readById(int id) {
       return repo.findById(id);
    }

    public V_etudiantecole update(int id, V_etudiantecole object) {
       try {

           Optional<V_etudiantecole> optional = repo.findById(id);
           if (optional.isPresent()) {
               V_etudiantecole existing = optional.get();
               existing = (V_etudiantecole) com.spring.utility.Utilitaire.addValueObject(existing,object);
               return repo.save(existing);
           }
       return null;
       }catch(Exception ex){
           ex.printStackTrace();
           return null;
       }
    }

    public void delete(int id) {
       repo.deleteById(id);
    }



}

