 package com.spring.service;

 import org.springframework.beans.factory.annotation.Autowired;
 import org.springframework.stereotype.Service;
 import com.spring.model.Retard;
 import com.spring.repository.RetardRepository;
 import java.util.List;
 import java.util.Optional;

 /*
    File generated By
    Christooj_Generation_Code
 */
 @Service
 public class RetardService {
    private final RetardRepository repo;

    public Integer getCount(int idannee,int idetudiant) {
      return repo.getCount(idannee,idetudiant);
   }

    @Autowired
    public RetardService(RetardRepository rep) {
        this.repo = rep;
    }

    public Retard create(Retard object) {
       return repo.save(object);
   }


    public List<Retard> read() {
       return repo.findAll();
    }

    public Optional<Retard> readById(int id) {
       return repo.findById(id);
    }

    public Retard update(int id, Retard object) {
       try {

           Optional<Retard> optional = repo.findById(id);
           if (optional.isPresent()) {
               Retard existing = optional.get();
               existing = (Retard) com.spring.utility.Utilitaire.addValueObject(existing,object);
               return repo.save(existing);
           }
       return null;
       }catch(Exception ex){
           ex.printStackTrace();
           return null;
       }
    }

    public void delete(int id) {
       repo.deleteById(id);
    }



}

