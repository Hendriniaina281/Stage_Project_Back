package com.spring.controller;

import com.spring.model.Academyusers;
import com.spring.model.Anneescolaire;
import com.spring.model.Trimestre;
import com.spring.model.Users;
import com.spring.service.AcademyusersService;
import com.spring.service.TokenService;
import com.spring.service.TrimestreService;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Optional;

/*
   File generated By
   Christooj_Generation_Code
*/
@RestController
@RequestMapping("/Trimestre")
@CrossOrigin("*")
public class TrimestreController{
   @Autowired
   private TrimestreService services;

   @Autowired
   private AcademyusersService acaUs;

    @Autowired
    TokenService servicetoken;

    @GetMapping("/findTrimestreProf/{idannee}/{idacademy}")
    public List<Trimestre> getTrimProf(@PathVariable int idannee,@PathVariable int idacademy) {
        try {
            List<Trimestre> all = services.getTrimestre(idacademy,idannee);
            return all;
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

   @GetMapping("/findTrimestreByid/{idannee}")
    public List<Trimestre> getTrimestre(@RequestHeader("Authorization") String token,@PathVariable int idannee) {
        try {
            Users user = servicetoken.getUser(token);
            Academyusers ac =  acaUs.findAcademyId(user.getId());
            List<Trimestre> all = services.getTrimestre(ac.getAcademy_id(),idannee);
            return all;
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }


   @PostMapping("/create")
    public String create(@RequestBody Trimestre object) {
    try {
        services.create(object);
        return "Cr��e avec succ�s";
    } catch (Exception e) {
        e.printStackTrace();
        return "Erreur lors de la cr�ation: " + e.getMessage();
    }
}

   @GetMapping("/find")
    public List<Trimestre> getAll() {
        try {
            List<Trimestre> all = services.read();
            return all;
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

   @GetMapping("/find/{id}")
    public Trimestre getById(@PathVariable int id) {
        try {
            Optional<Trimestre> optional = services.readById(id);
            return  optional.get();
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

   @PutMapping("/update/{id}")
    public Trimestre update(@PathVariable int id, @RequestBody Trimestre object) {
        try {
            Trimestre updated = services.update(id,object);
            return updated;
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

   @DeleteMapping("/delete/{id}")
    public String delete(@PathVariable int id) {
        try {
            services.delete(id);
            return "Success";
        } catch (Exception e) {
            e.printStackTrace();
           return "Il ya erreur: "+e.getMessage();
        }
    }


}

