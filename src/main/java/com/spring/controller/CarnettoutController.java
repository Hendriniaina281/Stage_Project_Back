package com.spring.controller;

import com.spring.model.Carnettout;
import com.spring.service.CarnettoutService;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Optional;

/*
   File generated By
   Christooj_Generation_Code
*/
@RestController
@RequestMapping("/Carnettout")
@CrossOrigin("*")
public class CarnettoutController{
   @Autowired
   private CarnettoutService services;

   @GetMapping("/getCarnetTout/{idecoleclasse}/{idanneescolaire}")
   public List<Carnettout> getCarnetTouts(@PathVariable int idecoleclasse,@PathVariable int idanneescolaire) {
       try {
           List<Carnettout> all = services.getCarnetTout(idecoleclasse, idanneescolaire);
           return all;
       } catch (Exception e) {
           e.printStackTrace();
           return null;
       }
   }


   @PostMapping("/create")
    public String create(@RequestBody Carnettout object) {
    try {
        services.create(object);
        return "Cr��e avec succ�s";
    } catch (Exception e) {
        e.printStackTrace();
        return "Erreur lors de la cr�ation: " + e.getMessage();
    }
}

   @GetMapping("/find")
    public List<Carnettout> getAll() {
        try {
            List<Carnettout> all = services.read();
            return all;
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

   @GetMapping("/find/{id}")
    public Carnettout getById(@PathVariable int id) {
        try {
            Optional<Carnettout> optional = services.readById(id);
            return  optional.get();
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

   @PutMapping("/update/{id}")
    public Carnettout update(@PathVariable int id, @RequestBody Carnettout object) {
        try {
            Carnettout updated = services.update(id,object);
            return updated;
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

   @DeleteMapping("/delete/{id}")
    public String delete(@PathVariable int id) {
        try {
            services.delete(id);
            return "Success";
        } catch (Exception e) {
            e.printStackTrace();
           return "Il ya erreur: "+e.getMessage();
        }
    }


}

