 package com.spring.repository;

 import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import org.springframework.stereotype.Repository;

 import com.spring.model.Anneescolaire;

 /*
    File generated By
    Christooj_Generation_Code
 */
 @Repository
 public interface AnneescolaireRepository extends JpaRepository<Anneescolaire, Integer>  {
 @Query(value = "select * from anneescolaire where extract(year from debutanneescolaire) = :debut and extract (year from finanneescolaire) = :fin and idecole = :ecoleid", nativeQuery = true)
   Anneescolaire getIdAnneScolaire(
       @Param("debut") int debut, 
       @Param("fin") int fin,
       @Param("ecoleid") int ecoleid
    );

   @Query(value = "SELECT * FROM anneeScolaire WHERE idecole = :ecole_id ORDER BY id DESC LIMIT 1" , nativeQuery = true)
    Anneescolaire findAnneesActif(Long ecole_id);

    List<Anneescolaire> findByIdecole(Integer idecole);











    
}

