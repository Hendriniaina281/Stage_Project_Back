 package com.spring.repository;

 import java.util.List;
 
 import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import com.spring.model.V_matiereclasse;

 /*
    File generated By
    Christooj_Generation_Code
 */
 @Repository
 public interface V_matiereclasseRepository extends JpaRepository<V_matiereclasse, Integer>  {
@Query(value = "select * from v_matiereclasse where idprof = :idprof and idanneescolaire = :idannee", nativeQuery = true)
   List<V_matiereclasse> findMatiereclasse(
       @Param("idprof") int idprof,
       @Param("idannee") int idannee
   );

   @Query(value = "select * from v_matiereclasse where ecoleclasseid = :ecoleclasseid and idanneescolaire = :idannee", nativeQuery = true)
   List<V_matiereclasse> getMatiereClasse(
       @Param("ecoleclasseid") int ecoleclasseid,
       @Param("idannee") int idannee
   );
}

